// 开发环境和生产环境的共同webpack配置
const utils = require('../utils')
const HtmlInjectConfigPlugin = require('../')
const core = require('../index')
const config = require('../config')()

module.exports = function (chain, theme, app) {

}